meta {
  name: Check Authentication
  type: http
  seq: 9
}

get {
  url: {{base_url}}/auth/check
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
}