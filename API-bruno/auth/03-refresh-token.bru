meta {
  name: Refresh Token
  type: http
  seq: 3
}

post {
  url: {{base_url}}/auth/refresh-token
  body: json
  auth: none
}

body {
  {
    "refreshToken": "{{refresh_token}}"
  }
}

settings {
  encodeUrl: true
}