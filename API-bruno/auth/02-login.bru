meta {
  name: Login User
  type: http
  seq: 2
}

post {
  url: {{base_url}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "admin-auth@example.com",
    "password": "hashed-password"
  }
}

settings {
  encodeUrl: true
}
