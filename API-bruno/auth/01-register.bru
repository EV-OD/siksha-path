meta {
  name: Register User
  type: http
  seq: 1
}

post {
  url: {{base_url}}/auth/register
  body: json
  auth: none
}

body:json {
  {
      "email": "admin-auth@example.com",
      "fullName": "Admin Auth",
      "role": "admin",
      "password": "hashed-password"
  }
}

settings {
  encodeUrl: true
}
