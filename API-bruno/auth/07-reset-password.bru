meta {
  name: Reset Password
  type: http
  seq: 7
}

post {
  url: {{base_url}}/auth/reset-password
  body: json
  auth: none
}

body {
  {
    "token": "reset-token-here",
    "newPassword": "newpassword123"
  }
}

settings {
  encodeUrl: true
}